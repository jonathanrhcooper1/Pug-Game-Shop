{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default offline app permissions for the initial shell.",
  "windows": ["main"],
  "permissions": [
    "shell:default"
  ]
}
