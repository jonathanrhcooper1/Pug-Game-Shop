{
  "name": "pug-game-shop-tcg-store-platform",
  "version": "0.4.0",
  "private": true,
  "description": "Trading card store platform development, testing, staging, and deployment tooling.",
  "scripts": {
    "wp-env:start": "wp-env start",
    "wp-env:stop": "wp-env stop",
    "wp-env:clean": "wp-env clean all",
    "wp-env:destroy": "wp-env destroy",
    "wp-env:reset": "wp-env clean all && wp-env start",
    "wp-env:seed": "node scripts/wp-env/seed-dev-data.mjs",
    "wp-env:test": "wp-env run tests-cli phpunit || node scripts/wp-env/report-pending-phpunit.mjs",
    "test": "npm run test:local && npm run test:required-matrix",
    "test:local": "cd apps/wordpress-plugin && php tests/run.php && php tests/bootstrap-smoke.php && php tests/lint.php",
    "test:required-matrix": "node scripts/wp-env/check-required-test-plan.mjs",
    "test:e2e": "playwright test",
    "test:wordpress": "npm run wp-env:start && npm run wp-env:test",
    "verify:no-production-secrets": "node scripts/wp-env/verify-no-production-secrets.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@wordpress/env": "^11.8.0"
  }
}
